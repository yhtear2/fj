<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="Comp">
	<insert id="insertComp" parameterType="dto.comp.CompDataBean">
		insert into FJ_COMP values(
			#{email, jdbcType=VARCHAR}, #{ceo, jdbcType=VARCHAR}, #{comp_part, jdbcType=VARCHAR}, #{tel},
			#{zipcode}, #{address, jdbcType=VARCHAR}, #{info, jdbcType=VARCHAR}, #{capital, jdbcType=VARCHAR},
			#{comp_type, jdbcType=VARCHAR}, #{incharge, jdbcType=VARCHAR}, #{comp_reg_date, jdbcType=VARCHAR}, #{year_sale},
			#{emp_count, jdbcType=VARCHAR}, #{like_count}, #{reg_date}, #{last_date}
		)
	</insert>
	
	<select id="selectComp" parameterType="String" resultType="dto.comp.CompDataBean">
		select * from FJ_COMP where email = #{email}
	</select>

	<update id="updateComp" parameterType="dto.comp.CompDataBean">
		UPDATE 	FJ_COMP
		SET 	CEO 			= #{ceo, jdbcType=VARCHAR}
				, COMP_TYPE 	= #{comp_type, jdbcType=VARCHAR}
				, TEL 			= #{tel, jdbcType=VARCHAR}
				, ZIPCODE 		= #{zipcode, jdbcType=VARCHAR}
				, ADDRESS 		= #{address, jdbcType=VARCHAR}
				, INFO 			= #{info, jdbcType=VARCHAR}
				, CAPITAL 		= #{capital, jdbcType=VARCHAR}
				, YEAR_SALE 	= #{year_sale, jdbcType=VARCHAR}
				, EMP_COUNT 	= #{emp_count, jdbcType=VARCHAR}
				, LIKE_COUNT 	= #{like_count, jdbcType=VARCHAR}
				, COMP_REG_DATE = #{comp_reg_date, jdbcType=VARCHAR}
				, LAST_DATE 	= SYSDATE
		WHERE 	EMAIL 			= #{email}
	</update>
	
	<delete id="deleteComp" parameterType="java.lang.String">
		DELETE
		FROM 	FJ_COMP
		WHERE 	EMAIL = #{email}
	</delete>
</mapper>	








