<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="Recruit">
	<select id="getRecruitCount" resultType="java.lang.Integer">
		SELECT 	COUNT(*)
		FROM 	FJ_RECRUIT
	</select>

	<select id="getRecruitList" resultType="dto.recruit.RecruitDataBean">
		<![CDATA[
		SELECT 	*
		FROM 	FJ_RECRUIT
		WHERE	ROWNUM >= #{start}
		AND 	ROWNUM <= #{end}
		]]> 
	</select>
	
	<insert id="insertRecruit" parameterType="dto.recruit.RecruitDataBean">
		insert into 
		FJ_RECRUIT values(
			FJ_Recruit_seq.NEXTVAL,
			#{email, jdbcType=VARCHAR},
			#{title, jdbcType=VARCHAR},
			#{content, jdbcType=VARCHAR},
			#{name, jdbcType=VARCHAR}, 
			#{skill, jdbcType=VARCHAR},
			#{gender, jdbcType=VARCHAR},
			#{edu, jdbcType=VARCHAR},
			#{work_type, jdbcType=VARCHAR},
			#{work_day, jdbcType=VARCHAR},
			#{work_area, jdbcType=VARCHAR},
			#{business_part, jdbcType=VARCHAR},
			#{incharge, jdbcType=VARCHAR},
			#{tel, jdbcType=VARCHAR},
			#{start_date, jdbcType=VARCHAR},
			#{end_date, jdbcType=VARCHAR},
			#{min_salary},
			#{max_salary},
			#{min_career},
			#{max_career},
			#{min_age},
			#{max_age},
			#{people_count},
			#{readcount},
			#{reg_date},
			#{last_date}
		)
	</insert>

</mapper>	








