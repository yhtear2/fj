<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="Message">
	<insert id="sendMessage"  parameterType="dto.message.MessageDataBean">
		insert into FJ_MESSAGE values( 
		FJ_MESSAGE_seq.NEXTVAL, #{email}, #{title, jdbcType=VARCHAR},
		 #{content, jdbcType=VARCHAR}, #{tag, jdbcType=VARCHAR}, 
		 #{read_yn}, #{reg_date})
	</insert>
	<select id="getCount" resultType="Integer">
		select count(*) from FJ_MESSAGE
	</select>
	<select id="getList" parameterType="String" resultType="dto.message.MessageDataBean">
		select * from FJ_MESSAGE where email=#{email}
	</select>
</mapper>	








